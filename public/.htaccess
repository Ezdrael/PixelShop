Options -Indexes
RewriteEngine On
RewriteBase /

# Якщо запит НЕ є існуючим файлом
RewriteCond %{REQUEST_FILENAME} !-f
# І якщо запит НЕ є існуючою папкою
RewriteCond %{REQUEST_FILENAME} !-d
# Перенаправити все на головний index.php
RewriteRule . index.php [L]